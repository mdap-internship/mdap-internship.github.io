# _config.yml
title: MDAP Internship Showcase

# Hero background rendered behind page content (see _layouts/default.html):
#   "datawave" — animated WebGL point-cloud (assets/js/datawave.js)
#   "image"    — static banner photo (assets/img/mdap-hero-banner.jpg)
# Flip this back to "image" if the WebGL visual ever causes rendering or
# performance issues — no code changes needed.
hero_background: datawave

# Whether interns added by hand under _past_interns/ (no repo of their own)
# appear on the home page, cohort pages and sidebar. Set to false to hide
# them without deleting their folders.
show_past_interns: false

defaults:
  - scope:
      path: "interns"
    values:
      layout: intern
  - scope:
      path: "cohorts"
    values:
      layout: cohort

exclude:
  - scripts/
  - Gemfile
  - Gemfile.lock
  - README.md
  - .github/
  - .ruby-version
  - vendor/bundle
  - vendor/cache
  - vendor/gems
  - vendor/ruby